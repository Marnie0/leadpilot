/**
 * How many leads the demo sandbox is seeded with.
 *
 * Copy on the landing and sign-in screens quotes this number, so it lives in
 * one place rather than being retyped wherever it is mentioned. It tracks
 * `server/prisma/seed.ts`.
 */
export const DEMO_LEAD_COUNT = 96;
